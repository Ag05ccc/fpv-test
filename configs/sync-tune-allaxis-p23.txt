# Sim-specific tune probe for the truthful-timing (looptime-sync) world.
# Roll gets the same P-only treatment the measured pitch/yaw sim profiles use;
# roll/pitch rates get the safe-manual-authority values. Yaw rates and the
# pitch/yaw PIDs are still applied over MSP by the runner flags, after this
# file is imported. Debug evidence only, not a real-flight tune.
set p_roll = 23
set i_roll = 0
set d_roll = 0
set roll_rc_rate = 5
set roll_srate = 30
set roll_rate_limit = 120
set pitch_rc_rate = 5
set pitch_srate = 30
set pitch_rate_limit = 120
